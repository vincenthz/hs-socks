# ~*~ auto-generated by haskell-ci with config : cdc3f31303432d1ba5ffdec4cd1159735d6d44bc21e1bfa8f3d7783e7da3b71f ~*~

version: "{build}"
clone_folder: C:\project
build: off
cache:
  - "C:\\SR -> .appveyor.yml"

environment:
  global:
    STACK_ROOT: "C:\\SR"
  matrix:
    - { BUILD: "ghc-8.6", STACKCMD: "stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps", STACKCFG: "{ resolver: lts-13.17, packages: [ '.' ], extra-deps: [], flags: {} }", STACKURL: "https://www.stackage.org/stack/windows-x86_64" }

matrix:
  fast_finish: true

install:
  - set PATH=C:\Program Files\Git\mingw64\bin;%PATH%
  - curl -ostack.zip -L %STACKURL%
  - 7z x stack.zip stack.exe
  - refreshenv
test_script:
  - echo %STACKCFG% > stack.yaml
  - stack setup > nul
  - echo "" | %STACKCMD%


